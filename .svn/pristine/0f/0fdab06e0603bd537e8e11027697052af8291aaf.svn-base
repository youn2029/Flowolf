/**
 *	정규식 
 */

// name
var nameReg = /^[A-z가-힣]{2,10}$/; 

// ID(Email)
var idReg =  /^[a-z0-9]+(\.[_a-z0-9]+)*@[a-z0-9-]+(\.[a-z0-9-]+)*(\.[a-z]{2,15})$/i;

// password (하나이상의 영문대문자,적어도 하나의 영문소문자, 하나 이상의 숫자, 하나 이상의 특수문자, 최소길이 8인치)
var pwReg = /^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$%^&*-]).{8,}$/;

// nickName
var nickReg = /^[ㄱ-ㅎ가-힣0-9]*$/;

// phone
var phoneReg = /^01([0|1|6|7|8|9]?)([0-9]{3,4})([0-9]{4})$/;